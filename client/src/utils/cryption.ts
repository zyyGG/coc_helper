// 加密模块

// aes 进行加密
export function cryptionRsa(str: string | number) {
  str = str.toString()
  return str
}

export function cryptionMd5(str: string | number) {
  str = str.toString()
  return str
}